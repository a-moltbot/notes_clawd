# notes_clawd

A tiny Hugo-powered notes site deployed to **GitHub Pages** via **GitHub Actions**.

## Local preview (optional)

If you have Hugo installed:

```bash
hugo server -D
```

## Deploy

Push to `main` and the included workflow will build & deploy to GitHub Pages.
